<!-- <nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">

    <div class="navbar-header">
      <a class="navbar-brand" href="/">Pharmabook</a>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

      <ul class="nav navbar-nav">
        <li class="active">
          <%= link_to("Products", products_path) %>
        </li>
        <li class="active">
          <%= link_to("APIs", apis_path) %>
        </li>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <li>
          <%= link_to carts_path do %>
            购物车 <i class="fa fa-shopping-cart"> </i> ( <%= current_cart.products.count %>)
          <% end %>
        </li>
        <li style="margin-right: 10px;">
                <%= form_tag search_products_path, method: :get do %>
                  <div class="input-group" id="search-id" >
                    <%= text_field_tag :search, params[:search], class: "form-control", placeholder: "搜索产品", id: "search", autocomplete: "off" %>
                    <span class="input-group-btn">
                      <button type="submit" class="btn btn-default btn-primary" name="nil">
                        <span class="glyphicon glyphicon-search"></span>
                      </button>
                    </span>
                  </div>
                <% end %>
              </li>
        <% if !current_user %>
          <li><%= link_to("Signup", new_user_registration_path) %> </li>
          <li><%= link_to(content_tag(:i, 'Login', class: 'fa fa-sign-in'), new_user_session_path) %></li>
        <% else %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Hi!, <%= current_user.email %>
              <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <% if current_user.admin? %>
                <li>
                  <%= link_to("Admin 选单", admin_products_path) %>
                </li>
              <% end %>
              <li>
                <%= link_to("个人订单列表", account_orders_path) %>
              </li>

              <li> <%= link_to(content_tag(:i, 'Logout', class: 'fa fa-sign-out'), destroy_user_session_path, method: :delete) %></li>
            </ul>
          </li>
          <% end %>
      </ul>

    </div>
  </div>
</nav> -->
<nav class="navbar navbar-default navbar-fixed-top navbar-style" role="navigation">
  <div class="container">
      <!--logo-->
      <div class="navbar-header">
        <a href="/" class="navbar-brand" style="font-size: 1.5em;">Pharmabook</a>

          <!-- 响应式导航 -->
          <button type="button" class="navbar-toggle" data-toggle="collapse"
                  data-target=".navbar-ex1-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
      </div>


      <!-- links -->
      <div class="navbar-collapse navbar-ex1-collapse collapse navbar-right" aria-expanded="false">

        <ul class="nav navbar-nav navbar-right">
          <li>
            <%= link_to("课程", "#") %>
          </li>
          <li>
            <%= link_to("关于我们", "#") %>
          </li>
          <li>
            <%= link_to carts_path do %>
              购物车 <i class="fa fa-shopping-cart"></i> ( <%= current_cart.products.count %> )
            <% end %>
          </li>
          <% if !current_user %>
            <li><%= link_to("注册", new_user_registration_path) %> </li>
            <li><%= link_to(content_tag(:i, " 登入", class: "fa fa-sign-in"), new_user_session_path) %></li>
          <% else %>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                Hi!, <%= current_user.email %>
                <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <% if current_user.admin? %>
                  <li><%= link_to("管理员", admin_products_path, class: "li-style") %></li>
                <% end %>
                <li><%= link_to("个人中心", account_orders_path, class: "li-style") %></li>
                <li><%= link_to("退出", destroy_user_session_path, method: :delete, class: "li-style") %></li>
              </ul>
            </li>
          <% end %>


        </ul>

        <ul class="nav navbar-nav navbar-right">
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              商城
              <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to("全部商品", products_path, class: "li-style") %></li>
              <li><%= link_to("收纳品", "#", class: "li-style") %></li>
              <li><%= link_to("装饰品", "#", class: "li-style") %></li>
            </ul>
          </li>
        </ul>

          <!-- <li>
            <%= link_to("课程", "#") %>
          </li>
          <li>
            <%= link_to("作业", "#") %>
          </li>
          <li>
            <%= link_to("论坛", "#") %>
          </li> -->

        <!-- 搜索输入框 -->
        <div class="navbar-form navbar-right">
            <div id="custom-search-input">
              <%= form_tag search_apis_path, :method => :get  do %>
                <div class="input-group col-md-12">
                    <input type="text" class="form-control input-sm" name="search" value="<%= params[:search] %>" placeholder="medicine name...">
                    <span class="input-group-btn">
                        <button class="btn btn-info btn-sm" type="submit">
                            <i class="glyphicon glyphicon-search"></i>
                        </button>
                    </span>
                </div>
              <% end %>
            </div>
        </div>

    </div>






  </div>
</nav>
